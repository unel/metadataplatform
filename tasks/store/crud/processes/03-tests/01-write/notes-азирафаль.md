---
agent: Азирафаль
step: 03-tests/01-write
date: 2026-04-29T18:23:20Z
---

[doc] Интерфейс логгера для fs.New — угадывал. Acceptance говорит "логгер подключён к конструктору", но не называет сигнатуру. Предположил `Log(level, msg string)`. Если реализация выберет другое имя или сигнатуру — тест сломается при компиляции. Это хорошо: тест зафиксирует контракт.

[doc] Интерфейс LogOp для router — аналогично. `LogOp(level, op, typ, id string, count int, errMsg string)` — это моё предположение об удобном структурированном логгере. Реализация может выбрать иначе.

[propose] Было бы полезно если acceptance явно указывала интерфейс логгера — хотя бы тип (структурированный vs строковый). Сейчас два агента (я и Кроули) можем принять разные решения.

[doc] uuid зависимость — нужно добавить `github.com/google/uuid v1.6+` в go.mod. Без этого nft_test.go не скомпилируется. Это намеренно: тест фиксирует требование к зависимости.

[doc] fs.Store реализует все три интерфейса (Entity, Relation, Job) — это предположение из spec-а. Compile-time assert в router_happy_test.go поймает отклонение.

[rework] t.Context() используется в nft_test.go — Go 1.21+, у нас 1.22, OK. Но если потребуется поддержка 1.20 — придётся переписать на context.Background().
