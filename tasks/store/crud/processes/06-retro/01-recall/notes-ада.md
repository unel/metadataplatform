[rework] Первый write-прогон завершился clarification — тесты happy/ предполагали архитектуру невозможную в Go; пришлось останавливаться и ждать fix upstream вместо движения вперёд.
[miss] report для 01-write не создан — существует только status-log.md; history первого прогона частично потеряна.
[miss] omitempty на Job.Payload прошёл через write-прогон незамеченным — нарушение контракта AC-JOB-UPSERT-01 поймал только ревьювер; значит читала spec невнимательно или spec не давала однозначного сигнала.
[friction] Ревьювер Гримм нашёл CRITICAL-1 (path traversal) — validateID проверял только пустую строку, хотя логика "id строится в путь файла" была очевидна с момента написания recordPath; invariant не был сформулирован явно, поэтому и не проверен.
[rework] WARNING-3 (372 строки в одном файле, троекратное дублирование upsert) — это не случайность, это следствие отсутствия generic-helper в первом проходе; написала три копии потому что дженерики казались избыточным усложнением для MVP, оказалось нарушением стандарта размера файла.
[propose] Лимит 150 строк на файл должен проверяться во время code-write, не на ревью — достаточно локального счётчика или pre-commit hook; сейчас это catch только на ревью.
[doc] recordPath после фикса CRITICAL-1 использует двойной filepath.Clean + HasPrefix; invariant "path не выходит за basedir" теперь задокументирован комментарием — этот паттерн стоит вынести в docs/standards/go/ как канонический способ защиты от path traversal.
[friction] string-based error matching (containsAny) — написала руками то что stdlib уже даёт; корень в том что sentinel errors не были заложены в types.go с самого начала, они появились на этапе фикса; следовало определить их вместе с типами.
[propose] Sentinel errors должны определяться в types.go или отдельном errors.go одновременно с интерфейсами — не в момент когда ревьювер находит string matching в router.
[whatever] TODO без тикета (store/fs/store.go:32) — противоречие между стандартом (требует тикет) и spec (требует именно такую формулировку); разрешить на уровне процесса до следующей фичи; сейчас это подвешенный договор ни о чём.
[miss] notes-ада.md и complaints-ада.md не были написаны в ходе работы над фичей — пишутся только сейчас на ретро; наблюдения тускнеют, детали теряются. Следующий раз писать по ходу, не откладывать.
[friction] Пришлось самостоятельно разобраться что 01-write завершился без report-001.md — документация процесса неполная; непонятно было ли это намеренное решение или просто упустили.
