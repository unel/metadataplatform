---
agent: Азирафаль
task: store/crud
date: 2026-05-05T06:30:08Z
---

[miss] Acceptance не описывала API интерфейсов: сигнатура логгера для fs.New, метод Handle роутера, структура store (три отдельных sub-store или один тип). Пришлось принимать архитектурные решения на этапе написания тестов — это не работа тест-агента.

[rework] Первоначальные тесты предполагали что *fs.Store реализует одновременно EntityStore+RelationStore+JobStore. В Go это невозможно — Get возвращает разные типы. Всё переписано через обёртки .Entities()/.Relations()/.Jobs(). Clarification поймал это поздно — на этапе 04-code, не на 03-tests. Правильное место для этого вопроса — acceptance.

[friction] t.Context() недоступен в Go 1.22, хотя я знал версию Go из go.mod. Прослеживается паттерн: знаю контекст, но проверяю поверхностно. Следующий раз — явно верифицировать совместимость API с версией компилятора.

[friction] uuid.Version(7) vs int(7) — молчаливое нарушение строгого сравнения типов testify. Нашёл только при запуске тестов с реальным компилятором. Это третья итерация fix. Хочется более ранней компиляционной проверки.

[rework] captureLogger в router_logging_test.go — сконструировал кастомный тип логгера для happy, adversarial использовал *slog.Logger. Несовместимость нашёл ревьювер, не я. Стоило с самого начала думать о совместимости типов между пакетами.

[propose] Если acceptance описывает что система "логирует" что-то — должна фиксировать конкретный интерфейс или хотя бы тип (structured slog vs fmt.Println). Иначе тест-агент угадывает и может угадать иначе чем code-агент.

[propose] Compile-time assertions — полезный паттерн, использовал var _ store.EntityStore = (*fs.Store)(nil). Стоит сделать это частью шаблона happy-тестов для любого пакета с интерфейсами.

[doc] Граничный случай TestEntity_Get_EmptyID_ReturnsError оказался дублем между happy и adversarial. В happy пустой ID — это граничный случай валидного вызова (boundary, не error path), в adversarial — ошибочный путь. Двойное именование запуталось. Конвенция: boundary-тесты пустого ID в happy, error-response тесты в adversarial — и имена должны это отражать.

[friction] 5 раундов review и 4 раунда fix — это много для одной подзадачи. Большая часть правок была предсказуема: несогласованные API между happy/adversarial, t.Context() версионная совместимость. Если бы существовал краткий checklist "перед сабмитом: проверь cross-package consistency" — два раунда отпали бы.

[propose] Краткий pre-submit checklist для тест-агентов: (1) все типы из одного пакета используют одинаковые интерфейсы; (2) API-вызовы совместимы с версией Go; (3) testify assert types строго совпадают; (4) нет тестов с silent return без assert.

[doc] Тест TestNFT_Atomic_UpsertInterruptedBySIGKILL_FileNotCorrupted — маркер-файл вместо time.Sleep, polling 5ms, timeout 5s. Это правильный паттерн для subprocess-тестов. Стоит задокументировать в tdd standards.

[whatever] После пяти раундов review у меня ощущение что review-процесс поймал то что должен был поймать, но слишком медленно. Вопрос не в качестве ревьювера — вопрос в том что тест-агент не имел достаточного контракта от предыдущего шага. Спека хорошая, acceptance подробная — но gap именно в API интерфейсов.
