[slow] Спека писалась очень долго — несколько прогонов ревью (5 итераций, CR-1—CR-21). Нужно разобраться почему так много циклов и как сократить.

[design] Спека привязана к стеку (Go-specific): конкретные типы, сигнатуры функций, паттерны вроде named return, defer, filepath.Join. Если завтра нужно переписать на другом языке — спека бесполезна. Надо подумать как разделить: интерфейсная/контрактная часть (независимая от стека) отдельно, Go-специфика отдельно.

[flow] Оркестратор останавливается после каждого шага и ждёт подтверждения — даже когда следующий шаг очевиден (failed → fix). Нужно двигаться автоматически: если статус failed и следующий шаг однозначен — запускать без паузы.

[retro] Ни один агент за всю фичу не написал ни одного замечания или жалобы к ретро (notes-<агент>.md и complaints-<агент>.md). Это странно — либо формат не донесли агентам, либо они не понимают зачем писать, либо пишут "чисто" чтобы не быть уязвимыми. Надо разобраться.

[structure] Нужно разобраться со структурой проекта — где что лежит, как устроены папки, нет ли дублирования. Сейчас структура непрозрачная.

[context] Надо поработать над эффективностью использования контекста — агенты тратят его нерационально, много повторений и шума.

[tooling] Надо подумать над серией мини-скриптов, которые облегчат выяснение статуса фичи и облегчат дозапись статус-лога и жалоб.

[timestamps] Время апдейтов в файлах процессов по-прежнему подозрительное — агенты, похоже, угадывают или hardcode-ят время вместо реального UTC. Нужно разобраться и зафиксировать как время правильно передаётся в субагенты.

[metadata] Формат метаданных в файлах процессов уплывает — поля то добавляются, то пропадают, структура frontmatter непоследовательна. Нужно стандартизировать и контролировать.

[go-version] Надо переехать на актуальную версию Go (1.26.2) — текущая версия устарела, обновление желательно до следующей фичи.

[state-log] Агенты должны записывать в стейт-лог не только итоговый статус, но и момент начала работы над шагом и момент завершения — чтобы можно было прикинуть сколько шаг занял времени и понять завершился ли он вообще (или завис).

[tests-ownership] Ада сама написала тесты в рамках `04-code` — это неприемлемо. Тесты должны писаться на шаге `03-tests` Кроули+Азирафалем, не разработчиком. Надо выяснить чего Аде не хватало: может, spec/AC были недостаточны, или тесты не запускались, или она не нашла тестовые файлы. Разобраться до следующей фичи.
