# Complaints: store/crud — Харли Куин (оркестратор)

Docs-fix: исправление одного Nit сломало пример ответа — добавили поля которые omitempty и в реальном ответе не появляются. 3 раунда ревью ради 4 документационных файлов — это многовато. Нет ли у нас проблемы с тем что исправляющий агент не проверяет смежные инварианты перед записью?

Танк без Write — это стабильная процедура, но каждый раз чувствуется как обход ограничения, а не нормальная работа. Я получаю контент в ответе агента, вручную создаю файлы. Если в ответе Танка будет опечатка — я её пропущу.

Сессия прервалась посреди 05-docs/03-fix (in-progress). Хорошо что status-log отражает состояние — можно было восстановиться. Но Танк с планом фикса уже не существовал — пришлось запускать заново с нуля, передавая весь контекст вторично.

Три итерации ревью документации из-за одной ошибки в одном примере раздражают. Это не системная проблема, но чувствуется что docs-fix не делает достаточного самоконтроля перед сабмитом.
