[rework] CR-9 — критический дедлок в graceful shutdown: cancel() вызывался после <-done, то есть никогда не вызывался пока горутины живы; баг появился во втором ревью потому что в первом acceptLoop был переписан и структура shutdown изменилась
[miss] тест TestStore_GracefulShutdown_ForcesCloseAfter5Seconds не ловит CR-9: sp.Stop() имеет собственный таймаут 6 секунд и тест завершается по таймауту обёртки, а не из-за поведения store; тест зелёный при сломанном коде
[miss] acceptance NFT-06 не специфицирует механизм принудительного закрытия — только observable outcome; тест написан под расплывчатую формулировку и не верифицирует внутренний контракт; поэтому не фальсифицируем при дедлоке
[friction] паттерн повторялся: CR-N исправлен → код переписан → в соседней зоне появляется CR-(N+2); CR-1 исправлен, acceptLoop переписан → CR-9; каждый рефакторинг вносил новый баг в смежную зону
[friction] CR-12 (паника при двойном SIGTERM) существовал с первой версии, но появился только в третьем ревью; main.go не попадал в фокус первых двух ревью
[propose] для graceful shutdown с принудительным закрытием — специфицировать в acceptance не только observable behaviour, но и контракт: "соединение закрывается до того как процесс завершается, а не вместе с завершением"; это делает тест фальсифицируемым при дедлоке
[propose] ввести в standards/testing.md требование: тест на timeout-based shutdown должен верифицировать что соединение закрыто именно по таймауту, а не по внешнему таймауту обёртки
[doc] isCleanClose семантически врёт: net.ErrClosed возникает при нашем conn.Close() изнутри, не при закрытии клиентом; логирует forced server-side close как clean client EOF; станет проблемой при диагностике in-flight транзакций в следующей фиче
[doc] ReadLine vs Scanner: выбор задокументирован только после CR-10; на момент написания кода комментария не было — следующий читатель потерял бы время
[whatever] CR-3 (двойной applyDefaults) принят as is — идемпотентно; если defaults получат conditional logic или side effects — двойной вызов создаст баг без очевидной причины
[whatever] FT-08/FT-09 тестируют одно и то же (оба через stopCh); ограничение архитектуры embedded runner; black-box тест с os.Process.Signal выходит за рамки фичи
[rework] CR-12 (паника при двойном сигнале) — баг существовал с первой версии, main.go не попал в фокус первых двух ревью; нашёлся только на третьей итерации; три файла в фиче, два отревьюированы дважды, третий — один раз
[rework] тесты NFT-06 и FT-08/09 пришлось переосмыслять не из-за логики, а из-за архитектурного ограничения embedded runner; black-box сигнальное тестирование не влезает в текущий тест-арсенал; это должно было быть известно до написания acceptance
[miss] в acceptance нет ни одного сценария для double-signal поведения (Ctrl+C дважды); реальный production path, не экзотика; CR-12 нашёлся только потому что ревью дошло до main.go
[miss] NFT-01 в test-review-2 — waitForMsg("conn_id") работал как time.Sleep(500ms) замаскированный под assertion; прошёл второй ревью только с пометкой "minor"; это false confidence
[friction] embedded runner архитектура тестов закрыла возможность полноценного тестирования сигналов; решение принято до написания acceptance, acceptance написан под ограничение, а не под требование
[friction] каждая итерация ревью выявляла замечание в зоне смежной с исправленным в предыдущей; acceptLoop → shutdown → main.go; предсказуемая цепочка которая должна быть в чеклисте рефакторинга горутин
[propose] при любом изменении горутинной структуры — обязательный повторный проход всего shutdown path целиком как отдельный шаг ревью, не часть общего прохода
[propose] ввести checklist для ревью Go-сервисов: main.go проверяется в каждом раунде, не только в первом; signal handling — отдельный пункт
[doc] embedded runner закрывает сигнальное тестирование — это архитектурное решение с последствиями; нужно зафиксировать в TECH.md тестового модуля что FT-08/09 не верифицируют signal.Notify path и почему
[miss] acceptance не содержит сценария для stale socket + невалидного пути одновременно — EDGE-04 проверяет несуществующую директорию, но нет сценария "сокет-файл существует но недостижим для bind"; NFT-02 покрывает только stale socket с нормальными правами
[rework] test-review прошёл два раунда; первый failed по 8 замечаниям из которых 7 исправлены; оставшееся FT-08/09 — структурное ограничение, не ошибка реализации; но тест-review-2 поставил passed с тремя оговорками — значит passed при незакрытых medium замечаниях; порог прохождения ревью размыт
[propose] ввести явный критерий прохождения test-review: passed только если нет open medium и выше; "passed с оговорками" при открытом medium — это failed с отложенным долгом
[doc] в spec отсутствует явное требование к max_connections — стандарт проекта требует ограничение, spec его не упоминает; CR-5 нашёл это через стандарт, но конфликт spec vs standards нигде не был явно разрешён; spec должна была включить max_connections или явно написать "вне MVP"
[friction] три итерации code-review при том что spec и acceptance были стабильны с первого ревью; все три итерации — следствие рефакторинга кода, не спорных требований; стандарты были доступны но не все применены с первого прохода
