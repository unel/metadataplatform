[rework] testhelpers пережил 4 итерации фиксов — waitForMsg("conn_id") изначально написан с неверным предположением что conn_id живёт в msg, а не в structured attrs; пришлось добавить waitForConnID и waitForNConnIDs
[rework] EDGE-01b: baseline фиксируется через waitForNConnIDs(i+1) чтобы гарантировать что лог connection-establishment уже записан; без этого счётчик плавал между итерациями
[rework] NFT-05 — SetLinger(0) для имитации IO-ошибки не работает на Unix sockets (только TCP); тест переписан под clean EOF, assertion изменён с INFO на DEBUG
[rework] TestHighThroughput_1000Lines_NoneDropped — deadlock при прогоне; unix socket buffer конечен, синхронная запись 1000 строк без параллельного чтения гарантированно заполняет его; запись вынесена в горутину
[friction] embedded runner усложнил тестирование SIGTERM/SIGINT — оба сигнала моделируются через close(stopCh); FT-08 и FT-09 технически тестируют одно и то же; coverage gap задокументирован в комментарии
[friction] ожидание готовности store требовало баланса — слишком короткий таймаут флакает, слишком длинный замедляет suite; итоговый подход (waitForMsg по пути сокета) работает но хрупок: зависит от формата лог-сообщения
[friction] NFT-06 в первой версии был нефальсифицируемым — если store завершался немедленно, тест проходил; потребовалось замечание от ревью и t.Fatal чтобы тест стал честным
[miss] acceptance не упоминала параллельность записи и чтения как требование к тест-клиенту; deadlock в TestHighThroughput был предсказуем из спеки, но в acceptance об этом ни слова
[miss] EDGE-04 покрыта в connection_test.go, но нет теста на graceful поведение при ошибке создания сокета в multi-socket конфиге — что происходит с уже поднятыми сокетами
[propose] стандарты тестирования дополнить разделом про full-duplex тест-клиенты: при тестировании протоколов с ответами запись и чтение должны идти параллельно; неочевидно и легко забыть
[propose] embedded runner — полезный паттерн, но его ограничения (нет настоящих сигналов) стоит задокументировать в docs/standards как известный trade-off
[propose] waitForMsg по содержимому лог-сообщения как механизм синхронизации — хрупкий контракт; если формат сообщения изменится, тест упадёт с неочевидной причиной
[doc] bufio.Scanner с MaxLineBytes: ErrTooLong не сообщает сколько байт прочитано до переполнения; поэтому спека говорит "точный размер не логируется"; стоит задокументировать как известное ограничение Scanner API
[doc] newTestScanner с буфером 4 МБ нужен потому что дефолтный Scanner имеет буфер 64 КБ, а тесты EDGE-02 работают с данными до 1 МБ + 1 байт
[whatever] TestGracefulShutdown_ForcesCloseAfter5Seconds добавлен skip при -short — тест по природе ждёт 5+ секунд, в CI без флага это незаметная задержка которая накапливается
[miss] acceptance содержит два отдельных сценария FT-08 и FT-09 (SIGTERM и SIGINT), но оба проходят через close(stopCh) — coverage gap не в тестах а в архитектуре runner; acceptance должна либо признавать это ограничение явно, либо требовать black-box тест через os.Process.Signal
[miss] acceptance не описывает поведение при частичной ошибке запуска multi-socket конфига — что происходит с сокетами которые успели подняться если один из них упал; EDGE-04 покрывает только один сокет
[friction] test-run-report выявил что ./store/... не является Go-пакетом (скомпилированный бинарник в корне) — паттерн для запуска тестов нетривиален, нужно запускать только ./cmd/store/...
[doc] unix socket buffer значительно меньше чем ожидаешь (~8 КБ для loopback против ~208 КБ для TCP loopback) — это прямая причина deadlock в TestHighThroughput; стоит задокументировать в standards как "Unix socket буферы малы, full-duplex тест-клиент обязателен"
[propose] acceptance для протоколов с ответами должна явно указывать что тест-клиент обязан читать ответы параллельно с записью; сейчас это implicit и легко пропустить даже зная спеку
[rework] NFT-01 — waitForMsg("conn_id") переписан на waitForConnID в test-fix-4 уже после второго ревью; ошибка воспроизвелась в двух разных тестах (FT-07 и NFT-01) — значит паттерн был неочевиден при первом написании testhelpers
