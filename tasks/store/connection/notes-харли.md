[miss] как субагент не имею доступа к Agent tool — не могу самостоятельно спаунить команду; приходится эскалировать к оператору вместо автономной оркестрации
[friction] recall пришлось запускать в три захода: сначала через Skill (не подошло), потом через Agent без bypassPermissions (агенты не смогли писать), потом оператор запустил напрямую; много итераций на базовую задачу
[propose] для recall и любых скиллов где агенты пишут файлы — явно прописывать bypassPermissions в инструкциях к оркестратору; без этого агенты дают правильный анализ но не могут его зафиксировать
[propose] у Харли-субагента должен быть доступ к Agent tool — иначе оркестрация возможна только на верхнем уровне, что ломает автономность флоу
[whatever] флоу recall занял несколько циклов вместо одного из-за отсутствия Agent tool у субагента; это системное ограничение платформы, не ошибка скилла
[rework] 3 раунда code-review на MVP echo-сервер — ревью шло по зонам (store.go → handler.go → main.go), а не полным проходом; CR-12 (паника при двойном сигнале) нашёлся только в 3м раунде
[friction] code-fix-1 потрачен на подтверждение уже исправленных замечаний — задание выдано без проверки что баги ещё актуальны на момент запуска
[miss] CR-9 (дедлок при shutdown) прошёл через test-write и test-review незамеченным — тест давал false positive; оркестратор не инициировал проверку корректности тестов
[miss] MaxConnections появился в коде (CR-5) без обновления spec.md и acceptance.md — оркестратор должен инициировать каскадное обновление при расширении scope
[propose] notes-user.md содержит ссылку на внешний CLAUDE.md — инициировать изучение и RFC если есть применимые стандарты; не оставлять как TODO
