---
purpose: Analyze для ретро store/crud
process: 06-retro/03-analyze
run: 1
date: 2026-05-05T11:45:33Z
created: 2026-05-05T11:45:33Z
status: done
agent: Харли Куин
team: retro-crud
---

## Группировка проблем

### Требования

- **API-контракт до test-write** — Азирафаль, Кроули, Гримм
  AC не содержит сигнатур конструкторов/интерфейсов. Два тест-агента угадывали независимо → несовместимые архитектуры → 4+ лишних раунда. *fs.Store не может реализовать три интерфейса напрямую — обнаружила Ада в 04-code.
  Root cause: нет раздела "API contracts" в шаблоне AC + action item из RETRO connection не имплементирован.

- **Спека Go-specific без разделения контракт/реализация** — Танк, Ада, Кроули
  filepath.Join, named return, конкретные сигнатуры смешаны с требованиями. CR-15 предписал паттерн без полного инварианта → CRITICAL-1 в code-review.
  Root cause: в шаблоне spec нет разделов "Контракт" и "Реализация на Go".

- **Инварианты не документируются** — Ада, Гримм
  Path traversal очевиден при написании recordPath, но не записан. Рецидив из store/connection.
  Root cause: в AC нет обязательного пункта "инварианты валидации" для операций с пользовательскими данными.

### Процессные

- **Action items retro не конвертируются в задачи** — Танк, Гримм, Кроули
  Из 8 пунктов RETRO connection → 0 выполнено. Ретро создаёт список без owner, дедлайна, backlog.
  Root cause: нет шага "создать задачи" в конце ретро.

- **Notes/complaints не пишутся по ходу** — все участники
  Ни один агент не писал в ходе фичи. Формат донесён только в 06-retro/01-recall.
  Root cause: notes/complaints не являются обязательным выходным артефактом шага в скиллах.

- **Порядок шагов нарушен: spec pending → code done** — Танк
  Ошибка оркестрации (подтверждено пользователем). Нужна gate-блокировка в pipeline.
  Root cause: оркестратор не проверяет статус вышестоящих шагов перед переходом.

- **Нет fix-report / дыры в истории** — Танк, Ада
  Рецидив из RETRO connection. report-001 для 04-code/01-write не создан.
  Root cause: тот же что "action items не конвертируются".

- **Граница 03-tests / 04-code размылась** — Кроули, Азирафаль
  Ада фиксила тесты в 04-code вместо clarification → 03-tests.
  Root cause: нет жёсткого правила о clarification при несовместимости тестов и реализации.

- **Каскадный сброс не валидируется** — Гримм
  test-review run-3 done, но прошли run-4 и run-5 — статус не откатился в stale.

- **Ретро при незакрытых шагах** — Гримм
  05-docs/02-review failed, ретро стартовало без явного решения о пропуске.

### Инструментальные

- **Compile-fail и test-fail неразличимы** — Кроули
  "setup failed" одинаково для Red phase и реальной ошибки компиляции.
  Решение: разделить на `go vet/build` и `go test`.

- **Ревьюер без shell** — Гримм
  Поиск report-NNN.md — угадывание. Частичное решение: оркестратор передаёт явный список файлов.

- **Чтение/запись статусов и notes — без инструментов** — пользователь
  Агенты пишут статусы вручную → появляется корневой status.md вместо status-log.md процессов. Барьер к ведению notes слишком высок.
  Решение: мини-скрипты `status <feature>`, `log-note <feature> <агент> "..."`, `log-complaint ...`.

### Командные

*(входят в требования и процессные)*
- Несогласованность тест-агентов: изоляция без сверки по API (кластер 1)
- Граница тест/реализатор (кластер 8)
- Потеря формата retro-артефактов (кластер 5)

## Открытые вопросы пользователю

**Кластер 6:** ошибка оркестрации — подтверждено. Gate-блокировку добавлять.

## Итого: 13 проблем, 4 категории

### Топ-3 для 04-solve

| Приоритет | Кластер | Обоснование |
|---|---|---|
| 🥇 | API-контракт в шаблоне AC | 4+ лишних итерации, 3 агента, рецидив |
| 🥈 | Retro backlog (action items → задачи) | системно повторяется, блокирует все остальные fixes |
| 🥉 | Мини-скрипты для status/notes | снижает барьер для всего остального |
