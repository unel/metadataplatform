# Notes: store/crud — Танк

[miss] Спека для store/crud не была написана вообще — все шаги (spec-ft, spec-nft, spec-review) остались в pending. Пайплайн двинулся в test-write и code без контракта. test-write/test-review/code-fix/code-review отмечены done при отсутствующей спеке — это структурная ошибка порядка шагов.

[friction] Нет process reports ни по одному шагу spec/acceptance/docs — директории 01-spec, 02-acceptance, 05-docs пустые или не существуют. Ретро по spec-роли невозможно провести без артефактов. Это системная проблема: агент завершил работу не оставив следов — либо файлы не записались, либо шаги не выполнялись вообще.

[propose] Если шаг spec-ft/spec-nft в статусе pending при переходе к test-write — оркестратор должен блокировать запуск, не двигаться дальше. Сейчас блокировки нет: код написан и отревьюен при pending-спеке.

[design] Спека store/connection была Go-specific: типы, сигнатуры, filepath.Join, named return. Для store/crud это должно быть исправлено до написания: контрактная часть (что делает, семантика операций, гарантии) отдельно от Go-специфики (как именно реализуется). Это позволяет ревьюить логику независимо от реализационных решений.

[doc] Шаблон спеки не содержит явного раздела "зависимости от стека". Пока его нет — Go-специфика будет просачиваться в ft.md/nft.md автоматически. Нужен явный раздел "реализационные решения" отдельно от "требований".

[propose] 21 замечание за 5 итераций ревью спеки (store/connection) — сигнал что либо первый черновик был слишком сырым, либо ревью-чек-лист слишком детальный и захватывает реализационные решения вместо требований. Нужно аудировать: сколько из 21 CR были про требования, сколько про стиль/формат/Go-специфику.

[miss] Action item из RETRO connection "acceptance расширяет спеку вместо верификации" — не был применён к store/crud (acceptance в pending).

[miss] Action item из RETRO connection "лог-формат не специфицирован" — статус неизвестен для crud. Без спеки неизвестно было ли это учтено в шаблоне или нет.

[friction] Notes/complaints для Танка не существовали до этого ретро. Формат донесён не был — ни через AGENT.md, ни через скилл. Агент пишет только если получил инструкцию писать.

[propose] Нужно встроить запись notes/complaints в скилл как обязательный финальный шаг, не опциональный.

[doc] status.md фичи store/crud: spec-ft pending, но test-write done и code-review done. Статусы не каскадируются при нарушении порядка. Нужно правило: если вышестоящий шаг в pending — нижестоящий не может быть done.

[whatever] tech-doc: failed — единственный шаг docs в статусе. Process report отсутствует. Неизвестно что именно провалилось и почему. Слепое пятно.
