[rework] CR-1: WaitGroup race — acceptLoop изначально мутировала внешний wg; пришлось перейти к локальному wg внутри acceptLoop + defer wg.Wait(); структура изменилась принципиально
[rework] CR-9: graceful shutdown дедлок — cancel() стоял после select, никогда не вызывался пока активные горутины живы; следствие того что CR-7 и CR-9 касаются одного блока — фикс одного создал другое
[rework] CR-13: isCleanClose разделена на isClientClose + isForcedClose в code-fix-5; семантика была неверна с самого начала: EPIPE и net.ErrClosed принципиально разные случаи
[friction] спека описывает поведение при ErrTooLong (bufio.Scanner), но реализация сделана на ReadLine (bufio.Reader); обработка переполнения принципиально отличается — расхождение стало явным только после CR-10
[friction] applyDefaults вызывается и в loadConfig, и в RunStore — CR-3 принят as is ("идемпотентно"); архитектурная неточность осталась намеренно; при следующей фиче кто-то наступит
[friction] code-fix-1: 4 из 5 описанных багов уже были исправлены на момент задания; итерация потрачена на подтверждение факта; задание не проверяло актуальность перед отправкой
[miss] тест FT-08/FT-09 через stopCh не тестирует реальный os.Signal path — coverage gap на SIGINT ветке; признан ограничением архитектуры, но остался незакрытым
[miss] MaxConnections добавлен через CR-5 (стандарт проекта), но не был в оригинальной спеке; конфиг и спека расходятся — нет обновления spec.md
[propose] CR-7 и CR-9 — взаимосвязанные изменения одного select-блока; замечания на одну точку кода лучше группировать в одном code-fix, не разбивать по итерациям
[propose] перед выдачей code-fix задания проверять текущее состояние кода относительно описанных проблем; code-fix-1 был потрачен почти вхолостую
[doc] ReadLine выбран намеренно: isPrefix даёт контроль накопления чанков без аллокаций при превышении лимита; Scanner не позволяет детектировать это без поглощения строки; нюанс должен быть в TECH.md
[doc] MaxConnections добавлен как требование стандарта, а не спеки — стоит зафиксировать в spec.md как дополнение
[whatever] shutdown-логика (cancel, wg, done-канал) размазана между RunStore и acceptLoop; при следующей фиче стоит рассмотреть вынос в отдельную структуру
[rework] CR-12: signal.Stop не был добавлен при первом написании main.go — паника при двойном Ctrl+C реальный production сценарий, который должен был быть закрыт на этапе написания, не ревью
[friction] 5 итераций code-fix при 3 раундах ревью; CR-12 и CR-13 не зависели от предыдущих исправлений и могли быть пойманы в code-review-1; волновой подход к ревью растянул цикл на итерацию-две
[friction] code-fix-2 закрыл 7 замечаний за раз — это много изменений одновременно; вероятность внести новый баг при такой плотности высокая; CR-9 появился именно после этой итерации
[miss] тест `TestStore_GracefulShutdown_ForcesCloseAfter5Seconds` маскировал дедлок CR-9 — завершался по таймауту обёртки (6 секунд), а не по реальному поведению store; тест который не проверяет то что называет — хуже чем отсутствие теста
[miss] CR-12 (паника при двойном сигнале) не имеет покрывающего теста — сигнальный path сложно тестировать, но хотя бы smoke через `signal.Stop` логику стоило зафиксировать
[propose] code-review выдавать за один раз весь список замечаний с приоритетами — не волнами; волны создают иллюзию прогресса при реальном накоплении долга
[doc] signal.Stop(sig) перед close(stopCh) — не очевидное решение; обоснование (второй сигнал в буфере канала) стоит держать в TECH.md или комментарии рядом с кодом
[miss] spec.md не обновился после добавления MaxConnections через CR-5 — конфиг-пример в спеке не содержит max_connections, хотя поле теперь реально существует и имеет дефолт; drift между спекой и реализацией с первого же ревью
[rework] handler.go переписывался трижды: ReadLine + isPrefix комментарий (fix-3), ветка ErrUnexpectedEOF (fix-1), разделение isCleanClose (fix-5) — файл трогался в каждой итерации кроме fix-2; нет связности изменений
[friction] тест который маскирует баг хуже чем отсутствие теста — TestStore_GracefulShutdown_ForcesCloseAfter5Seconds прошёл через все ревью с дедлоком внутри; нужен стандарт: тест должен завершаться именно по тому условию которое проверяет, а не по таймауту обёртки
[propose] EDGE-04 (директория не существует) и NFT-02 (stale socket) — граничные случаи lifecycle сокета; их стоит вынести в отдельный блок спеки "lifecycle сокет-файла" с явным порядком операций, сейчас раскиданы по разным разделам
[doc] порядок операций при shutdown (stop listener → remove socket → wait goroutines) явно описан в спеке; код следует этому порядку, но без явного комментария в коде; следующему разработчику не очевидно что порядок намеренный и продиктован спекой
