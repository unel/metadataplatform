[doc] `max_line_bytes` задокументирован как перебиваемый через $MAX_LINE_BYTES, но в PROJECT.md такой ENV переменной нет — нужно зафиксировать явно, не только в спеке
[miss] в spec.md не описан формат лог-записей: структура, поля (JSON? plain text? slog?) — имплементатор выбирал сам
[miss] нет требования что именно пишется в лог при успешном приёме соединения: conn_id и путь сокета — сборный образ из разных мест спеки; стоило дать пример лог-строки
[miss] exit code при принудительном завершении после таймаута graceful shutdown не определён явно — NFT-06 говорит "код 0" даже при принудительном разрыве; осознанное решение, нигде не обосновано
[friction] граница "нормальное закрытие (EOF)" vs "IO-ошибка" в Go неочевидна (io.EOF vs io.ErrUnexpectedEOF vs net.ErrClosed); спека не уточняет что считать "неожиданным" — реализатор трактует по-разному
[friction] NFT-04 ("клиент закрывает соединение в момент когда store пишет ответ") — условие гонки, трудно воспроизводимо детерминированно; acceptance-критерий реалистичен, но тест будет флакать без синхронизации
[doc] конфиг-пример включает `ops` на сокет, но фильтрация по `ops` явно исключена как следующая фича — конфиг читается, поле парсится, но не используется; нужна явная оговорка что поле игнорируется в этой итерации
[doc] в spec.md упомянуто sync/atomic для conn_id без уточнения типа; для Go 1.22 лучше зафиксировать `atomic.Uint64` явно
[propose] при следующей фиче (разбор ops) вернуться к спеке и добавить: что store делает с полем `ops` в конфиге — логирует ли при старте, в каком виде
[whatever] README.md для задачи отсутствует; постановка дошла только через содержимое спеки — нет источника первоначального запроса, нет контекста "зачем именно сейчас"
[miss] в PROJECT.md конфиг store не содержит `max_line_bytes` — спека добавила поле без синхронизации с PROJECT.md; расхождение между двумя источниками истины
[friction] EDGE-04 (директория для сокета не существует) отсутствовала в первоначальных требованиях — появилась в acceptance, но в spec.md в разделе fail-fast не зафиксирована; acceptance добавила новый failure mode который спека не покрывала
[miss] спека не определяет порядок создания сокетов при старте: последовательно или параллельно; если один сокет упал при создании — что происходит с уже созданными (cleanup или leak)
[propose] для следующих фич в спеке фиксировать лог-формат явно с примером строки — устраняет категорию споров "запись есть, но не та"
[doc] PROJECT.md описывает конфиг store без `max_line_bytes`, только с `db_url` и `sockets` — этот параметр живёт только в spec.md; при ревью PROJECT.md реализатор не узнает о нём
[miss] поведение heartbeat/пустых строк появилось в спеке без явного источника — это assumption, не требование от оператора; нужно было либо спросить, либо пометить как decision с обоснованием
[rework] порядок shutdown-операций (пп 1-3 в спеке) пришлось расписывать явно — без него "graceful shutdown" трактуется по-разному: сокет удаляется до или после ожидания горутин
[whatever] status.md не содержит fix-report-ов по спеке и acceptance — невозможно восстановить сколько итераций ревью было и что именно менялось; история потеряна
[miss] спека не фиксирует идемпотентность операции остановки (Stop) — test-run показал double-close в cleanup; деталь реализации, но контракт остановки стоило назвать явно
[friction] status.md упоминает spec-ft и spec-nft как отдельные шаги, но в задаче один файл spec.md — расхождение между статусами и реальной структурой файлов
