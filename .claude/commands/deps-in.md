_Исполнитель: агент **Герман** (`herman`)._

Анализ только входящих зависимостей фичи.

Аргументы: `$ARGUMENTS` — путь к фиче, например `store/query`

Выполняет `/deps` но использует только `find-incoming-spec.md` и `find-incoming-code.md` файлы.
Показывает только кто зависит от этой фичи.

См. полное описание в `deps.md`.
