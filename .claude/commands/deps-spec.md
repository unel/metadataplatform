_Исполнитель: агент **Бо** (`Бо`)._

Анализ зависимостей фичи только по спекам.

Аргументы: `$ARGUMENTS` — путь к фиче, например `store/query`

Выполняет `/deps` но использует только `find-outgoing-spec.md` и `find-incoming-spec.md` файлы.
Не анализирует код.

См. полное описание в `deps.md`.
