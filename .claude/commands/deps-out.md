_Исполнитель: агент **Герман** (`herman`)._

Анализ только исходящих зависимостей фичи.

Аргументы: `$ARGUMENTS` — путь к фиче, например `store/query`

Выполняет `/deps` но использует только `find-outgoing-spec.md` и `find-outgoing-code.md` файлы.
Показывает только от чего зависит эта фича.

См. полное описание в `deps.md`.
