_Исполнитель: агент **Герман** (`herman`)._

Анализ зависимостей фичи только по коду.

Аргументы: `$ARGUMENTS` — путь к фиче, например `store/query`

Выполняет `/deps` но использует только `find-outgoing-code.md` и `find-incoming-code.md` файлы.
Не анализирует спеки.

См. полное описание в `deps.md`.
