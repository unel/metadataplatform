# Поиск входящих extends по коду

## Go
- grep по имени структуры этой фичи в embedding других пакетов
- grep по имени интерфейса этой фичи в реализациях других пакетов

## SvelteKit

### lib/components/
- grep по имени компонента в slot-обёртках других компонентов
- grep по имени Props типа в `extends` других компонентов

### lib/stores/
- grep по имени стора в `derived()` других сторов

### lib/types/
- grep по имени типа в `extends` и пересечениях (`&`) других типов

## CSS
- grep по имени CSS класса или custom property в переопределениях других компонентов
