# Поиск входящих extends по спеке

Ищи в `spec.md` других фич признаки что они строятся поверх этой:

- фразы "расширяет X", "специализирует X", "добавляет к X" где X — эта фича
- описание частного случая поведения этой фичи
- переопределение части поведения этой фичи
