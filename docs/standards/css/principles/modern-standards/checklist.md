- [ ] Цвета, отступы, типографика — через custom properties (--var)
- [ ] Сложные layout'ы — через grid/flexbox, не float
- [ ] Медиа-запросы — через @media, контейнерные — через @container
- [ ] Слои специфичности явно управляются через @layer
- [ ] Перед использованием новой фичи — проверка на caniuse.com или web.dev/baseline
