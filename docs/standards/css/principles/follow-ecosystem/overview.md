# Следи за развитием CSS

CSS развивается быстрее чем кажется — за последние годы появились container queries, @layer, :has(), color-mix() и многое другое. То что раньше требовало JS или препроцессора — теперь нативный CSS.
