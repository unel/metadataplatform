# Type vs Interface

`type` и `interface` во многом взаимозаменяемы, но у каждого есть своя область применения. Консистентность важнее идеального выбора — главное договориться и следовать соглашению.
