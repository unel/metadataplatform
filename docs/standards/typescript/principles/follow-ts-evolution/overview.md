# Следи за эволюцией TypeScript

TypeScript активно развивается — каждый релиз добавляет новые возможности вывода типов. Периодически пересматривай существующий код: возможно то что раньше требовало сложного workaround теперь решается нативно.
