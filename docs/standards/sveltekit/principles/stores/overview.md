# Stores vs Runes

Svelte 5 рекомендует руны как основной механизм реактивности. Stores остаются для глобального состояния которое нужно разделять между несвязанными компонентами. Не используй stores там где хватает рун.
