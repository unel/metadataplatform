- [ ] Поиск по содержимому через `@>`, не через `->>` + CAST
- [ ] GIN-индекс на jsonb колонках с частым поиском
- [ ] JSON path (`@@`, `@?`) для сложных условий вместо множества `->>` операторов
- [ ] Нет конкатенации строк для формирования jsonb условий — только параметры
- [ ] При update — `||` оператор или `jsonb_set`, не перезапись всего объекта
