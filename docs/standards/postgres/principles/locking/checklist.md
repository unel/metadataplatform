- [ ] Queue pattern: `FOR UPDATE SKIP LOCKED` вместо SELECT + UPDATE в два шага
- [ ] Advisory locks для singleton-гарантий между инстансами приложения
- [ ] Блокировки берутся в одном порядке во всём приложении — предотвращает дедлоки
- [ ] Транзакции держатся минимально короткими — не делать HTTP-запросы внутри транзакции
- [ ] `NOWAIT` вместо ожидания когда блокировка — признак конфликта, а не нормальный флоу
