# Блокировки в PostgreSQL

PostgreSQL предоставляет несколько механизмов блокировки: row-level locks (FOR UPDATE), advisory locks для координации между процессами. Неправильное использование ведёт к дедлокам или потере производительности.
