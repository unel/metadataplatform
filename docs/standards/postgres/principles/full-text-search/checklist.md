- [ ] `tsvector` колонка обновляется триггером или при записи — не вычисляется в запросе
- [ ] GIN-индекс на `tsvector` колонке
- [ ] Запросы через `@@` оператор, не LIKE/ILIKE для full-text
- [ ] `plainto_tsquery` или `websearch_to_tsquery` для пользовательского ввода — не `to_tsquery` напрямую
- [ ] Ранжирование через `ts_rank` когда нужен relevance sort
- [ ] Один язык конфигурации по всему приложению (`simple` или конкретный язык)
