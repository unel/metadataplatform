- [ ] Нет N+1 — связанные данные загружаются одним запросом
- [ ] EXPLAIN ANALYZE запущен для нетривиальных запросов
- [ ] Pagination через cursor (WHERE id > $cursor), не OFFSET для больших таблиц
- [ ] SELECT * не используется — только нужные колонки
- [ ] Транзакции используются для атомарных операций
- [ ] Нет запросов внутри циклов
