- [ ] Store не знает об API, spawner, workers — только хранит и отдаёт данные
- [ ] Бизнес-логика не содержит HTTP-кода, SQL, файловых операций
- [ ] Зависимости между пакетами идут в одном направлении — нет циклических импортов
- [ ] Слой можно заменить не трогая другие — интерфейс остаётся тем же
- [ ] Нижние слои тестируются без верхних
