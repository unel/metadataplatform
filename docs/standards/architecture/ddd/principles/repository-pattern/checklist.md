- [ ] Только `cmd/store` содержит SQL и знает о PostgreSQL
- [ ] Остальные бинари работают через store socket — JSONL-протокол
- [ ] Интерфейс store оперирует доменными объектами: `entity`, `relation`, `job` — не строками и не `map`
- [ ] Детали хранения (индексы, транзакции, курсоры) не просачиваются за пределы store
- [ ] Store можно подменить другой реализацией не меняя клиентов
