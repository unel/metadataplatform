- [ ] Компонент взаимодействует с другими только через явный интерфейс (сокет, API, интерфейс)
- [ ] Нет прямого импорта кода другого компонента минуя его публичный интерфейс
- [ ] Worker не знает о БД — читает/пишет только через stdout JSONL
- [ ] Spawner не реализует бизнес-логику — только оркестрирует
- [ ] API не обходит store — все данные через store socket
