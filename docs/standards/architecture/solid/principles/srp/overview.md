# Single Responsibility Principle

У модуля должна быть ровно одна причина для изменения — ровно один стейкхолдер или actor, чьи требования он реализует.

Признак нарушения: модуль приходится менять по двум несвязанным причинам.
