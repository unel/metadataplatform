# Duplication vs Wrong Abstraction

Дублирование — зло. Но неправильная абстракция хуже дублирования. Если абстракция не подходит — лучше скопировать и разойтись, чем насиловать общий код параметрами и флагами.
